# Porta do servidor
PORT=3000

# JWT para autenticação (trocar em produção)
JWT_SECRET=troque-isto-em-producao

# IA opcional (LLM)
# Para ativar, crie um .env (não versionado) com a sua chave real
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
